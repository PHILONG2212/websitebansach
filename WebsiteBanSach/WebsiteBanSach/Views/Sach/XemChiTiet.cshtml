@using WebsiteBanSach.Models
@using webbansach.Model;
@model Sach
@{
    ViewBag.Title = "XemChiTiet";
    Layout = "~/Views/Layout/NguoiDungLayout.cshtml";
}
@section noidungbenphai{
    
<div ><img src="@Url.Content("~/HinhAnhSP/"+Model.AnhBia)" height="250px" width="200px" /></divstyle>

    <div style="margin-top:20px;">Tên sách:  @Model.TenSach</div>
<div>Mã chủ đề:  @ViewBag.TenChuDe</div>
<div>Mã nhà xuất bản:  @ViewBag.NhaXuatBan</div>
<div>Nôi dung tóm tắt: @Model.MoTa</div>
    <div>Đơn giá: @string.Format("{0,0:0}", Model.GiaBan) VNĐ</div>

    </br>
    @using (Html.BeginForm("ThemGioHang", "GioHang", new { @iMaSach = Model.MaSach, @strURL = Request.Url.ToString() }))
    {
        
    <input type="submit" value="Mua hàng" />
    }
    
    
}
<style>
    .activity {
        display:none;
    }
    #slider {
        display:none;
    }
    li {
        margin: 10px;
        font-size: 20px;
        height: 40px;
        width: 40px;
        border: 1px solid;
        text-align: center;
        background-color: white;
    }

        li:hover {
            margin: 10px;
            font-size: 20px;
            height: 40px;
            width: 40px;
            border: 1px solid;
            text-align: center;
            background-color: lightgray;
            color: black;
        }
</style>